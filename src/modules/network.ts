import { Actions } from "../types/core.js";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
async function checkNetwork(advices: string[], actions: Actions) {
  // TODO: check network stats
}

export { checkNetwork };
